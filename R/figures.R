# Two primary visuals:
# 1. Feature importance / SHAP summary, with cross-fold variance shown.
# 2. Ozone partial dependence plot.
#
# TODO: load model outputs and save figures to figures/ (ggsave())

library(ggplot2)

make_figures <- function() {
  stop("Generate the two primary visuals once models are fit.")
}

make_figures()
